{% macro method(m) -%}
    {{ "static" if m.is_static() else "" }}
    {{ m.return_value.cpp_name() }}
    {{ m.get_name() }}(
        {% for p in m.params %}
        {{- p.cpp_name() }}{{ "," if not loop.last else "" }}
        {% endfor -%}
    );
{%- endmacro %}
