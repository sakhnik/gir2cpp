#pragma once

#include <{{ ns.c_include }}>

namespace gir {
namespace {{ ns.name }} {
{% for k,v in ns.aliases.items() %}
using {{ k }} = {{ v }};
{%- endfor %}

} //namespace {{ ns.name }};
} //namespace gir;
