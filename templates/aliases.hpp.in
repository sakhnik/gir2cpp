#pragma once

{% for i in ns.c_includes %}
#include <{{ i }}>
{%- endfor %}

namespace gir {
namespace {{ ns.name }} {
{% for k,v in ns.aliases.items() %}
using {{ k }} = {{ v }};
{%- endfor %}

} //namespace {{ ns.name }};
} //namespace gir;
